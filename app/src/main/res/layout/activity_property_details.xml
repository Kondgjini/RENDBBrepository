<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Property details header -->
    <TextView
        android:id="@+id/propertyDetailsText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Property Details"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- Custom calendar displayed via a GridView -->
    <GridView
        android:id="@+id/calendarGridView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:numColumns="7"
        android:verticalSpacing="4dp"
        android:horizontalSpacing="4dp"
        android:padding="4dp"
        android:layout_marginBottom="16dp" />

    <!-- Booking controls container (placed at the bottom) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- TextView to display the selected booking period -->
        <TextView
            android:id="@+id/bookingPeriodText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Selected booking period will appear here"
            android:layout_marginBottom="16dp" />

        <!-- Button to select the start date -->
        <Button
            android:id="@+id/selectStartDateButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select Start Date"
            android:layout_marginBottom="8dp" />

        <!-- Button to select the end date -->
        <Button
            android:id="@+id/selectEndDateButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select End Date"
            android:layout_marginBottom="8dp" />

        <!-- Button to confirm booking -->
        <Button
            android:id="@+id/bookNowButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Book Now"/>
    </LinearLayout>
</LinearLayout>
