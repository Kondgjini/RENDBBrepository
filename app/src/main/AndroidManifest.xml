<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rendbb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RENDBB">

        <activity android:name=".views.AddPropertyActivity" />
        <activity android:name=".views.ManageBookingsActivity" />
        <activity android:name=".views.LoginActivity" />
        <activity android:name=".views.RegisterActivity" android:exported="true"/>
        <activity android:name=".views.ManagerDashboardActivity" />
        <activity android:name=".views.PropertyManagementActivity" />
        <activity
            android:name=".views.PropertyDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".receivers.StatusNotificationReceiver" />


    </application>
</manifest>
